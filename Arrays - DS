
this is the solution for hackerrank problem


n = int(input().strip())
arr = [int(arr_temp) for arr_temp in input().strip().split(' ')]

# Reverse arr and convert integer to string
rarr = map(str, arr[::-1])
print(" ".join(rarr))

for more array reversing types :

list = []
n = int(input("enter the number of elements"))
for i in range(0,n):
    ele = int(input())
    
    list.append(ele)
print(list)    
list.reverse()
print(list)


list = []
n = int(input("enter the number of elements"))
for i in range(0,n):
    ele = int(input())
    
    list.append(ele)
print(list)    
list[::-1]




